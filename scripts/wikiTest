# This script runs the commands that are described on the wiki page.
station make client c1 c2 c3
station make server s1
station connect s1 c1 c2 c3
station keys add c1 c2 c1 c3 c2 c3

station c1 send "it's my birthday!"
# From here on it depends on the network how fast the message is transmitted. 
# Therefore the rest can not be done within this script until blocking is available again.
echo "You should now see a result if you type 'station c3 read'"